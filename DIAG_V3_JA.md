# 右手前ボタン：内部プルアップ比較診断 v3

## 目的
従来の v2 で右手前の R7 が押下・解放とも 0、左手前の L8 が 1→0→1 だった状態を調べます。
裸の基板で実施。今回は両方の入力に内部プルアップを追加し、左手前を比較対象にします。
これは診断用であり修理ファームではありません。実機での動作は未確認です。

## ファーム
Endgame_1.5.3_pullup_diagnostic_v3.uf2
元ソース：Endgame 1.5.3、commit 099d79d8bd7800169dc5b1b86ee6f17aea4d2d75。
変更：buttons.dtsi の btns 5=P0.07 と btns 4=P0.08 に GPIO_PULL_UP を追加。
通常の GPIO 入力設定経路を使用し、GPIO を出力に切り替える処理は追加しません。
キー割当は変更しません。設定消去処理は追加しません。
UF2 は 0x1000 以上 0x6C000 未満のアプリ領域であることをビルド時に検証します。

## 確認手順
1. PuTTY を閉じる。
2. これまでと同じ方法で TRACKBALL ドライブを表示し、v3 の UF2 をコピーする。コピー後は自動再起動を待つ。
3. PuTTY を Serial、以前ログが出ていた COM6（変わった場合は新しいログ用 COM）、115200、8N1、フロー制御 None で開く。
4. endgame-diag3$ または EGPULL3 の表示を確認。EGRAW2 だけなら旧版。
5. 何も押さず 5 秒待つ。
6. 右手前を 2 秒押して 2 秒離す。3 回繰り返す。
7. 左手前も同じ操作をする。
8. 前後の EGPULL3 行を含むログを共有する。通常クリック動作ではなく R7/L8 を見る。

ログ例（数値は例、実測ではない）：
EGPULL3 R7=1 L8=1 cfg7=0000000c cfg8=0000000c PU7=1 PU8=1

PU7/PU8 は実際の PIN_CNF の入力方向・入力接続・プルアップ設定が期待通りかを示します。
1 は期待設定、0 は期待と異なる設定。レジスタ上の設定確認であり抵抗そのものの健全性の証明ではありません。
raw 入力は 100ms ごとに読み、変化時と約 2 秒ごとに表示します。
GPIO の SENSE 等が変わると cfg 全体が 0000000c と異なることがあります。PU7/PU8 も併せて確認します。

## 判断の限界
- PU7=1 かつ R7 が解放1→押下0→解放1：この条件では入力経路が機能。外部引き上げ回路や接触等の追加確認へ。
- PU7=1 かつ R7 が常に0：短絡、漏れ、入力故障等が残る。部品箇所までは断定不可。
- PU7=1 かつ R7 が常に1：断線と即断しない。内部プルアップと基板の直列抵抗の分圧で押下時にも十分低くならない場合がある。
- 左側も押下時0にならない：この試験条件自体の影響が疑われ、右側の故障判定には使えない。
- PU7/PU8=0：想定設定が維持されていないため、先に設定競合や休止状態を調査する。

公開ハード設計 rev10 は参考資料で、ユーザーの rev08 と完全同一とは未確認です。
診断終了後は前の v2 または元の 1.5.3 を書き戻してください。通常使用を続けるためのファームではありません。

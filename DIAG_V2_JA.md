# 直接入力診断 v2

1.5.3を基に、通常のEGDIAG1押下ログに加えて、nRF52833のGPIO入力レジスタと端子設定レジスタを読み取ります。100msごとに読み取り、値・設定の変化時または約2秒ごとに表示します。読み取り専用で、GPIOを出力にしたりプルアップを変更したりしません。端子のアナログ電圧や導通は測れません。

ビルド成功とUF2検査完了後に使用してください。実機未検証です。

## 手順

1. PuTTYを閉じ、通常のUF2書き込み手順でv2を書き込みます。
2. USBで接続し、PuTTY COM6 / 115200 / 8N1 / Flow control Noneを開きます。COM番号が変わった場合はデバイスマネージャーで確認します。
3. Enterで `endgame-diag2$` が出ることを確認します。
4. 何も押さず約3秒待ちます。
5. 正常な左手前ボタンを2秒押して離します。
6. 右手前ボタンを2秒押して離します。2回繰り返します。
7. `EGRAW2` の行と通常の `EGDIAG1` 行を含むスクリーンショットを確認します。

例（実測ではありません）:

```
EGRAW2 R7=1 L8=1 cfg7=00030000 cfg8=00030000
```

R7は右手前P0.07、L8は左手前P0.08の生のデジタル入力値です。正常なアクティブロー入力では通常、離すと1、押すと0です。cfg値は入力接続・方向・プル・割り込みの検出設定等を含みます。値全体の違いだけで異常と断定せずビットごとに確認します。

## 次に確認する順序

- R7も1/0で変わるのにbutton=7が出ない: スキャン/割り込み/デバウンス経路を確認。
- R7が一定で端子が入力切断・出力等: まず設定とその変更元を確認。電気故障と決めない。
- L8が正常に変化し、R7は適切な入力設定なのに一定: 組立状態の押し込みを確認した後、信号経路（はんだ、配線、直列抵抗、短絡、周辺部品）を優先して調査。
- 配線側が確認できてからマイコン端子の損傷を検討。

この診断だけで、接続不良・短絡・マイコン損傷を個別に確定することはできません。100ms未満の変化は取り逃がすため、ボタンは2秒保持します。通常のスキャンと電源管理を残しているので、入力が切断されている場合はcfgを確認します。

入力レジスタを読む遅延ワークは既存システムワークキューを使用します。診断負荷が追加されるため、滑らかさの性能評価には使いません。
